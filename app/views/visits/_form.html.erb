<%= form_for(@visit) do |f| %>
  <% if @visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% @visit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :PersonID %><br>
    <%= f.number_field :PersonID %>
  </div>
  <div class="field">
    <%= f.label :Date %><br>
    <%= f.datetime_select :Date %>
  </div>
  <div class="field">
    <%= f.label :MedicalStuffID %><br>
    <%= f.number_field :MedicalStuffID %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
